# SOS report (Support-Oriented Services)

When our mechine got into trouble then when we raise the issue with linux like redhat they want all our system conf, packages, etc to verify. So we cant tell them everything so this **`sosreport`** cmd will zip all requirements in one tar file and keep in directory (that will specify .) and we can share that for them thats more than enough for them.